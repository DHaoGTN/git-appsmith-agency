export default {

	checkIsTokenExist: function() {
		// var token = "";
		// token = appsmith.store.token;
		// var countToken = check_token_exist.run(token);
		// showAlert(0, 'info');
		// if (countToken == 1) {
		// navigateTo('HomePage');
		// }
	},
	startRepeat: function() {
		// setInterval(this.checkIsTokenExist, 8000);
	}
}
